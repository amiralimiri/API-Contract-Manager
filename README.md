# API-Contract-Manager
A production-ready backend platform built with FastAPI, Docker, and Kubernetes for managing data contracts, validating schema versions, generating diffs, and preventing breaking changes across microservices. Includes automated mock servers, contract testing, and schema versioning workflow.
